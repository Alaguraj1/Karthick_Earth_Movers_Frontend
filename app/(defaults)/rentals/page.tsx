import RentalManagement from '@/components/stone-mine/rentals/rental-management';

export const metadata = {
    title: 'Rental Management | Karthick Earth Movers',
    description: 'Manage vehicle and machine rentals — track day/night vehicle hire and per-hour machine rentals.',
};

const RentalPage = () => {
    return <RentalManagement />;
};

export default RentalPage;
